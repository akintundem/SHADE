# Project rules

- **Env files:** Do not create any `.env.*` files (e.g. `.env.prod`, `.env.local`). We only use `.env.example`. Copy it to `.env` locally and fill in values; never add new env filenames.
